workflows:
  ios-workflow:
    name: iOS Build
    max_build_duration: 60
    instance_type: mac_mini_m1
    environment:
      flutter: stable
      xcode: latest
      cocoapods: default
    scripts:
      - name: Get Flutter packages
        script: |
          flutter packages pub get
      - name: Install pods
        script: |
          find . -name "Podfile" -exec dir {} \;
          cd ios && pod install && cd ..
      - name: Build iOS
        script: |
          flutter build ios --release --no-codesign
      - name: Package IPA
        script: |
          mkdir -p Payload
          mv build/ios/iphoneos/Runner.app Payload/
          zip -r Runner.ipa Payload
    artifacts:
      - Runner.ipa
      - build/ios/iphoneos/*.app
    publishing:
      email:
        recipients:
          - ftrejopaz@gmail.com  # <--- CAMBIA ESTO POR TU EMAIL